{% extends 'base.twig' %}

{% block content %}
{# {{dump(post)}} #}
    <article
        class="article post-type-{{ post.post_type }}"
        id="post-{{ post.ID }}"
    >
       {% include 'pages/_partials/_card.twig' %}
    </article>

    {% endblock %}